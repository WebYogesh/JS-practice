const name = "Yogesh"
const repoCount = 5

console.log (`Hello my name is ${name} and my repo cout is ${repoCount}`);

const getName = new String('Yogesh Pathak')

console.log (getName.toUpperCase());
console.log (getName.charAt(2));
console.log (getName.indexOf('t'));

const newString = getName.slice(-12, 4)
console.log (newString);

